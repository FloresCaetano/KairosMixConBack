spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kairosmix_db
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

logging:
  level:
    root: DEBUG
    org.springframework: DEBUG